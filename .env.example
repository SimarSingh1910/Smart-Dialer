# Copy to .env and adjust. Only the DSN normally needs changing.
SMARTDIALER_DSN=postgresql://smartdialer:smartdialer@localhost:5432/smartdialer

# Optional overrides (defaults shown)
# SMARTDIALER_TICK_SECONDS=0.25
# SMARTDIALER_LEASE_SECONDS=30
# SMARTDIALER_LOG_LEVEL=INFO
